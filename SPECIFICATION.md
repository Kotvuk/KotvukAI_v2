# KotvukAI — Полная спецификация проекта для доработки

> Этот документ содержит ВСЮ информацию для полной доработки проекта. Следуй ему как инструкции от А до Я.

---

## 1. Обзор проекта

**KotvukAI** — веб-платформа для автоматического анализа криптовалютных графиков с помощью ИИ. Аналог TradingView с встроенным AI-аналитиком.

- **Целевая аудитория:** от новичков до профессиональных трейдеров
- **Монетизация:** 3 тарифа — Free / Pro ($29/мес) / Premium ($99/мес)
- **Цель:** привлечение инвестиций
- **Ключевое преимущество:** AI анализирует ВСЁ что видит пользователь (графики, сделки, калькулятор, китов) и даёт контекстные рекомендации в реальном времени
- **Языки интерфейса:** Русский (по умолчанию), English

---

## 2. Технологический стек

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| Frontend | React + JSX | 18.x |
| Сборка | Vite | 5.x |
| Графики | TradingView Lightweight Charts | 4.x |
| Backend | Express.js | 4.x |
| База данных | SQLite (better-sqlite3) | .db файл |
| AI | Groq API (Llama 3.1 8B Instant) | OpenAI-compatible |
| Данные рынка | Binance Public API | v3 |
| Индекс страха | Alternative.me API | v1 |
| Архивация | archiver | для ZIP скачивания |

---

## 3. Структура проекта

```
KotvukAI/
├── server.js                  # Express сервер (API + статика)
├── package.json               # Зависимости
├── vite.config.mjs            # Конфиг Vite
├── index.html                 # HTML точка входа
├── crypto_analytics.db        # SQLite база (генерируется автоматически)
├── .gitignore
├── README.md
├── SPECIFICATION.md           # Этот файл
├── src/
│   ├── main.jsx               # Точка входа React (LangProvider обёртка)
│   ├── App.jsx                # Главный компонент (навигация, роутинг панелей)
│   ├── i18n.js                # Переводы RU/EN
│   ├── LangContext.jsx        # React Context для языка
│   └── panels/
│       ├── ChartsPanel.jsx    # ✅ Графики (реализовано)
│       ├── AIPanel.jsx        # ✅ AI Аналитика (реализовано)
│       ├── AIChat.jsx         # ✅ Плавающий AI чат (реализовано)
│       ├── LearningPanel.jsx  # ✅ Обучение (реализовано)
│       ├── SettingsPanel.jsx  # ✅ Настройки (реализовано)
│       ├── Placeholder.jsx    # Заглушка для нереализованных панелей
│       ├── TradesPanel.jsx    # ❌ НУЖНО СОЗДАТЬ
│       ├── CalculatorPanel.jsx# ❌ НУЖНО СОЗДАТЬ
│       ├── WhalePanel.jsx     # ❌ НУЖНО СОЗДАТЬ
│       ├── AlertsPanel.jsx    # ❌ НУЖНО СОЗДАТЬ
│       ├── NewsPanel.jsx      # ❌ НУЖНО СОЗДАТЬ
│       └── WatchlistPanel.jsx # ❌ НУЖНО СОЗДАТЬ
└── dist/                      # Сборка (генерируется vite build)
```

---

## 4. Архитектура

```
Пользователь (браузер)
    ↕ HTTP
Express Server (порт 3000)
    ├── Статические файлы (React SPA из /dist)
    ├── /api/klines → Binance API (свечи)
    ├── /api/ticker24h → Binance API (тикеры 24ч)
    ├── /api/fng → Alternative.me (страх/жадность)
    ├── /api/ai/analyze → Groq API (анализ монеты)
    ├── /api/ai/chat → Groq API (чат)
    ├── /api/signals → SQLite (сигналы)
    └── /api/download-project → ZIP архив проекта
```

> **ВАЖНО:** Сервер выступает прокси для всех внешних API. Фронтенд НИКОГДА не обращается к Binance/Groq напрямую. Это защищает API ключи и обходит CORS.

---

## 5. Что уже реализовано

| Функция | Статус | Описание |
|---------|--------|----------|
| 📊 Графики | ✅ Готово | Свечи/Линия/Бары, 7 таймфреймов, 10 пар, авто-обновление 10 сек, объёмы |
| 🤖 AI Аналитика | ✅ Готово | Индекс страха, обзор рынка, AI сигналы через Groq, кнопка "Почему?" |
| 💬 AI Чат | ✅ Готово | Плавающая кнопка 💬, окно чата, история, Groq API |
| 📚 Обучение | ✅ Готово | 8 уроков, раскрывающиеся карточки, реальный контент |
| ⚙️ Настройки | ✅ Готово | Язык RU/EN, профиль, отображение тарифов |
| 🌐 i18n | ✅ Готово | React Context, файл переводов, переключатель |
| 🍔 Навигация | ✅ Готово | Бургер-меню, сайдбар, все панели |
| 🗄️ SQLite | ✅ Готово | Таблицы users, signals, settings + 3 тарифа |
| 📦 ZIP скачивание | ✅ Готово | GET /api/download-project |
| 📋 Сделки | ❌ Заглушка | Нужно реализовать |
| 🧮 Калькулятор | ❌ Заглушка | Нужно реализовать |
| 🐋 Анализ китов | ❌ Заглушка | Нужно реализовать |
| 🔔 Алерты | ❌ Заглушка | Нужно реализовать |
| 📰 Новости | ❌ Заглушка | Нужно реализовать |
| 🏆 Избранное | ❌ Заглушка | Нужно реализовать |

---

## 6. Что нужно доработать (полный список по приоритетам)

> 🔴 **КРИТИЧНО:** Все данные должны быть РЕАЛЬНЫМИ. Никаких демо/мок данных. Все API вызовы к реальным сервисам.

### Приоритет 1 — Основные панели
1. **📋 TradesPanel.jsx** — журнал сделок с историей
2. **🧮 CalculatorPanel.jsx** — расчёт входа в сделку
3. **🐋 WhalePanel.jsx** — крупные движения
4. **🔔 AlertsPanel.jsx** — уведомления по уровням
5. **📰 NewsPanel.jsx** — крипто-новости с AI саммари
6. **🏆 WatchlistPanel.jsx** — watchlist

### Приоритет 2 — Улучшение графиков
1. Индикаторы на графике: RSI, MACD, Bollinger Bands, EMA (9, 21, 50, 200), SMA, Volume
2. Инструменты рисования: горизонтальные линии, трендовые линии, Фибоначчи
3. Мультичарт: 2-4 графика одновременно
4. Полноэкранный режим графика

### Приоритет 3 — AI система
1. AI видит все открытые панели пользователя (кроме настроек) и учитывает контекст
2. Реалтайм AI анализ (автоматический, без кнопки) для Premium
3. Анализ скриншотов (загрузка изображения → AI распознаёт паттерны)
4. Улучшить промпт для более точных сигналов

### Приоритет 4 — Авторизация и безопасность
1. Регистрация/вход (email + пароль)
2. JWT токены для сессий
3. Ограничение API вызовов по тарифу
4. Хеширование паролей (bcrypt)

### Приоритет 5 — Полировка
1. PWA (Progressive Web App) — установка на телефон
2. Реферальная система
3. Демо-режим без регистрации (показать BTC анализ)
4. Экспорт в PDF/CSV
5. Светлая тема
6. Анимации и переходы

---

## 7. Детальное описание каждой панели

### 📊 Графики (ChartsPanel.jsx) — ✅ ГОТОВО, нужна доработка

**Текущее состояние:** свечи/линия/бары, 7 таймфреймов (1m, 5m, 15m, 1h, 4h, 1d, 1w), 10 пар, автообновление 10 сек, объёмы.

**Что доработать:**
- **Индикаторы** — выпадающий список для добавления: RSI (отдельная панель внизу), MACD (гистограмма внизу), Bollinger Bands (на графике), EMA 9/21/50/200 (линии на графике), SMA, Volume Profile
- **Инструменты рисования** — панель слева: горизонтальная линия, трендовая линия, прямоугольник, уровни Фибоначчи, текстовая метка. Рисование мышкой. Сохранение в SQLite
- **Мультичарт** — кнопка "2x2" для разделения экрана на 2-4 графика
- **Полноэкранный режим** — кнопка раскрытия
- **Больше пар** — поиск по всем парам Binance
- **Хейкен-Аши** — дополнительный тип графика

**Библиотека:** `lightweight-charts` от TradingView. Документация: https://tradingview.github.io/lightweight-charts/

**API:** `GET /api/klines?symbol=BTCUSDT&interval=1h&limit=500` → проксирует `https://api.binance.com/api/v3/klines`

---

### 🤖 AI Аналитика (AIPanel.jsx) — ✅ ГОТОВО, нужна доработка

**Текущее состояние:** 4 карточки метрик, таблица обзора рынка, кнопка AI анализ, модальное окно "Почему?".

**Что доработать:**
- **Анализ TOTAL** — общая капитализация. API: `https://api.coingecko.com/api/v3/global` (бесплатно). Показывать: Total Market Cap, BTC Dominance, ETH Dominance, 24h Volume
- **Улучшенные сигналы** — вместо 24h change использовать реальные klines данные, считать EMA/SMA кроссоверы, RSI
- **Тех. анализ блок** — тренд, RSI, MACD, уровни поддержки/сопротивления
- **Фундаментальный анализ блок** — market cap, circulating supply, доминация
- **Кнопка "Почему?"** — детальный модал: все параметры, пошаговое объяснение от AI, уровень риска, альтернативные сценарии
- **AI видит контекст** — при анализе отправлять данные всех панелей (watchlist, открытые сделки, калькулятор, киты)

---

### 📋 Сделки (TradesPanel.jsx) — ❌ НУЖНО СОЗДАТЬ

**Описание:** Журнал сделок пользователя. Виртуальные сделки (paper trading).

**Функционал:**
- **Открытие сделки** — форма: пара, направление (Long/Short), размер позиции, цена входа (автозаполнение текущей), TP, SL
- **Список открытых сделок** — таблица: пара, направление, вход, текущая цена, P&L (реальное время, обновление 10 сек), TP, SL, кнопка "Закрыть"
- **История закрытых сделок** — таблица: пара, направление, вход, выход, P&L, даты, причина закрытия (TP/SL/вручную)
- **Статистика** — карточки: общий P&L, % выигрышных, средний P&L, лучшая/худшая сделка
- **Авто-закрытие** — сервер каждые 10 сек проверяет TP/SL и закрывает автоматически

**SQLite таблица:**
```sql
CREATE TABLE trades (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  pair TEXT NOT NULL,
  direction TEXT NOT NULL,  -- 'long' или 'short'
  entry_price REAL NOT NULL,
  exit_price REAL,
  quantity REAL NOT NULL,
  tp REAL,
  sl REAL,
  pnl REAL,
  status TEXT DEFAULT 'open',  -- 'open', 'closed_tp', 'closed_sl', 'closed_manual'
  opened_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  closed_at DATETIME
);
```

**API:**
- `GET /api/trades` — список (фильтр: status=open/closed)
- `POST /api/trades` — открыть сделку
- `POST /api/trades/:id/close` — закрыть вручную
- `GET /api/trades/stats` — статистика

---

### 🧮 Калькулятор (CalculatorPanel.jsx) — ❌ НУЖНО СОЗДАТЬ

**Описание:** Калькулятор для расчёта входа в сделку и риск-менеджмента.

**Поля ввода:**
- Размер депозита ($)
- Риск на сделку (% от депозита, по умолчанию 1-2%)
- Цена входа
- Стоп-лосс (цена или %)
- Тейк-профит (цена или %)
- Плечо (1x, 2x, 5x, 10x, 20x, 50x, 100x)

**Автоматический расчёт:**
- Размер позиции ($)
- Количество монет
- Максимальный убыток ($)
- Потенциальная прибыль ($)
- Соотношение риск/прибыль (Risk/Reward Ratio)
- Цена ликвидации (при использовании плеча)
- Комиссия (0.1% по умолчанию)

**Визуализация:** Цветная шкала риска (красный → зелёный). Предупреждение если риск > 5% депозита.

**AI контекст:** Когда пользователь настраивает калькулятор, AI видит эти параметры и учитывает в рекомендациях.

---

### 🐋 Анализ китов (WhalePanel.jsx) — ❌ НУЖНО СОЗДАТЬ

**Описание:** Мониторинг крупных транзакций и движений "китов".

**Источники данных:**
- Binance API — крупные ордера: `GET /api/v3/depth?symbol=BTCUSDT&limit=50`
- Аггрегированные сделки: `GET /api/v3/aggTrades?symbol=BTCUSDT&limit=100` — фильтр > $100,000
- Whale Alert API (если будет ключ): `https://api.whale-alert.io/v1/transactions`

**Отображение:**
- **Лента китов** — список крупных транзакций: время, пара, объём ($), тип (покупка/продажа)
- **Order Book Heatmap** — визуализация крупных ордеров (стены покупок/продаж)
- **Сводка** — карточки: объём крупных покупок/продаж за 24ч, соотношение buy/sell, тренд
- **AI анализ** — интерпретация: "Киты активно покупают BTC — возможен рост"

**API сервера:**
- `GET /api/whale/orderbook?symbol=BTCUSDT` — крупные ордера
- `GET /api/whale/trades?symbol=BTCUSDT&minUsd=100000` — крупные сделки

---

### 🔔 Алерты (AlertsPanel.jsx) — ❌ НУЖНО СОЗДАТЬ

**Описание:** Уведомления когда цена/индикатор достигает уровня.

**Типы алертов:**
- Цена выше / ниже уровня
- Изменение за период > X%
- RSI выше 70 / ниже 30
- Пересечение EMA

**Функционал:**
- Форма создания: пара, условие, значение, сообщение
- Список активных алертов (с кнопкой удалить)
- История сработавших
- Toast/popup уведомление + звук (опционально)

**SQLite таблица:**
```sql
CREATE TABLE alerts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  pair TEXT NOT NULL,
  condition TEXT NOT NULL,  -- 'price_above', 'price_below', 'change_above', 'rsi_above', 'rsi_below'
  value REAL NOT NULL,
  message TEXT,
  status TEXT DEFAULT 'active',  -- 'active', 'triggered', 'disabled'
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  triggered_at DATETIME
);
```

**Серверная проверка:** Каждые 10 сек сервер проверяет все активные алерты и помечает сработавшие.

**API:**
- `GET /api/alerts` — список
- `POST /api/alerts` — создать
- `DELETE /api/alerts/:id` — удалить
- `GET /api/alerts/triggered` — сработавшие

---

### 📰 Новости (NewsPanel.jsx) — ❌ НУЖНО СОЗДАТЬ

**Описание:** Лента крипто-новостей с AI-суммаризацией.

**Источники (бесплатные):**
- CryptoCompare: `https://min-api.cryptocompare.com/data/v2/news/?lang=EN` (без ключа)
- CryptoPanic: `https://cryptopanic.com/api/free/v1/posts/?auth_token=FREE&public=true&kind=news`

**Отображение:**
- Список новостей: заголовок, источник, время, превью, ссылка
- Фильтр по монете (BTC, ETH, все)
- Кнопка "AI Саммари" → Groq суммаризирует на русском
- Sentiment-метка: позитивная/негативная/нейтральная (AI определяет)

**API:**
- `GET /api/news` — список новостей
- `POST /api/news/summary` — AI суммаризация

---

### 🏆 Избранное (WatchlistPanel.jsx) — ❌ НУЖНО СОЗДАТЬ

**Описание:** Персональный watchlist.

**Функционал:**
- Добавление пар (поиск по тикеру)
- Таблица: пара, цена, 24h%, мини-график (sparkline), объём
- Drag & drop сортировка
- Клик → переход на графики с этой парой
- Автообновление 10 сек

**SQLite таблица:**
```sql
CREATE TABLE watchlist (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  pair TEXT NOT NULL,
  position INTEGER DEFAULT 0,
  added_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**API:**
- `GET /api/watchlist` — список
- `POST /api/watchlist` — добавить
- `DELETE /api/watchlist/:id` — удалить
- `PUT /api/watchlist/reorder` — порядок

---

### 📚 Обучение (LearningPanel.jsx) — ✅ ГОТОВО

8 уроков с реальным контентом. Бесплатно для всех. Можно улучшить: тесты/квизы, прогресс-бар.

---

### ⚙️ Настройки (SettingsPanel.jsx) — ✅ ГОТОВО, нужна доработка

**Текущее:** Язык RU/EN, профиль, тарифы.

**Доработать:**
- Сохранение настроек в SQLite
- Тема (тёмная/светлая)
- Таймзона
- Валюта (USD, EUR, RUB)
- Интервал обновления данных
- Оплата тарифа (Stripe)
- Кнопка "📦 Скачать проект"

---

## 8. AI система

### API подключение
```
URL: https://api.groq.com/openai/v1/chat/completions
API Key: YOUR_GROQ_API_KEY_HERE
Model: llama-3.1-8b-instant
Max tokens: 2000 (анализ), 1500 (чат)
Temperature: 0.7
```

### AI должен видеть (всё кроме настроек):
- 📊 Графики — какая пара, таймфрейм, последние N свечей
- 📋 Сделки — открытые позиции, P&L
- 🧮 Калькулятор — депозит, риск, плечо
- 🐋 Киты — последние крупные транзакции
- 🔔 Алерты — активные алерты
- 📰 Новости — заголовки последних новостей
- 🏆 Watchlist — список отслеживаемых пар
- ❌ Настройки — НЕ передавать (приватность)

### Контекстный промпт (пример):
```
"Пользователь смотрит BTC/USDT на 4h.
У него открыта Long позиция по ETH на $2000 с P&L +5%.
В калькуляторе: депозит $10000, риск 2%, плечо 5x.
Киты: за час крупные покупки BTC на $50M.
Watchlist: BTC, ETH, SOL.
Индекс страха: 25 (Крайний страх).

На основе всего контекста дай рекомендацию."
```

### Системные промпты:

**Анализ монеты (/api/ai/analyze):**
```
"Ты — профессиональный крипто-аналитик. Отвечай подробно на русском языке. Используй markdown. Давай конкретные цифры для входа, TP и SL."
```

**Чат (/api/ai/chat):**
```
"Ты — AI помощник на платформе KotvukAI. Отвечай на вопросы о криптовалютах, трейдинге, анализе. Будь полезным и дружелюбным. Отвечай на языке вопроса."
```

---

## 9. База данных (SQLite)

**Файл:** `crypto_analytics.db` в корне проекта  
**Просмотр:** DB Browser for SQLite

### Все таблицы:

```sql
-- Существующие
CREATE TABLE users (id INTEGER PRIMARY KEY AUTOINCREMENT, email TEXT, name TEXT, password_hash TEXT, plan TEXT DEFAULT 'Free', created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE signals (id INTEGER PRIMARY KEY AUTOINCREMENT, pair TEXT, type TEXT, entry REAL, tp REAL, sl REAL, reason TEXT, accuracy REAL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE settings (id INTEGER PRIMARY KEY AUTOINCREMENT, key TEXT UNIQUE, value TEXT);

-- Нужно добавить
CREATE TABLE trades (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, pair TEXT, direction TEXT, entry_price REAL, exit_price REAL, quantity REAL, tp REAL, sl REAL, pnl REAL, status TEXT DEFAULT 'open', opened_at DATETIME DEFAULT CURRENT_TIMESTAMP, closed_at DATETIME);
CREATE TABLE alerts (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, pair TEXT, condition TEXT, value REAL, message TEXT, status TEXT DEFAULT 'active', created_at DATETIME DEFAULT CURRENT_TIMESTAMP, triggered_at DATETIME);
CREATE TABLE watchlist (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, pair TEXT, position INTEGER DEFAULT 0, added_at DATETIME DEFAULT CURRENT_TIMESTAMP);
CREATE TABLE user_settings (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, key TEXT, value TEXT, UNIQUE(user_id, key));
CREATE TABLE drawings (id INTEGER PRIMARY KEY AUTOINCREMENT, user_id INTEGER, pair TEXT, timeframe TEXT, type TEXT, data TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);
```

---

## 10. Авторизация и тарифы

### Тарифные планы

| Функция | Free (бесплатно) | Pro ($29/мес) | Premium ($99/мес) |
|---------|------------------|---------------|-------------------|
| Графики | Базовые | Все типы | Все + мультичарт |
| Индикаторы | 3 базовых | Все | Все + кастомные |
| AI анализ | 5/день | 50/день | Безлимит |
| AI Чат | 10 сообщ/день | 100 сообщ/день | Безлимит |
| Кнопка "Почему?" | ❌ | ✅ | ✅ |
| Анализ китов | ❌ | ✅ | ✅ |
| Алерты | 3 активных | 20 активных | Безлимит |
| Watchlist | 5 пар | 20 пар | Безлимит |
| Новости AI саммари | ❌ | ✅ | ✅ |
| Реалтайм AI | ❌ | ❌ | ✅ |
| Мультичарт | ❌ | ❌ | ✅ |
| Инструменты рисования | Линии | Все | Все |
| Экспорт PDF/CSV | ❌ | ✅ | ✅ |
| Приоритетная поддержка | ❌ | ❌ | ✅ |

### Авторизация (нужно реализовать)
- Регистрация: email + пароль (bcrypt)
- Вход: email + пароль → JWT токен
- Middleware проверки токена
- Middleware проверки тарифа (лимиты)
- Страница входа/регистрации

---

## 11. Мультиязычность (i18n)

**Реализация:** React Context (`LangContext.jsx`) + файл переводов (`i18n.js`)

**Использование:**
```jsx
const { t, lang, setLang } = useLang();
// В JSX:
<h1>{t('charts')}</h1>
```

**Языки:** RU (по умолчанию), EN

**Правило:** При добавлении новых панелей — добавлять все строки в оба языка в `i18n.js`. AI промпты должны учитывать текущий язык.

---

## 12. Дизайн-система

### Цвета
| Элемент | Цвет |
|---------|------|
| Фон страницы | `#0a0a0f` |
| Фон сайдбара | `#0d0d14` |
| Фон карточек | `#12121a` |
| Фон инпутов | `#1a1a2e` |
| Акцент | `#3b82f6` (синий) |
| Рост | `#26a69a` (графики) / `#22c55e` (бейджи) |
| Падение | `#ef5350` (графики) / `#ef4444` (бейджи) |
| Нейтральный | `#eab308` |
| Текст основной | `#e0e0e0` |
| Текст вторичный | `#a0a0b0` |
| Текст приглушённый | `#666` |
| Бордеры | `rgba(255,255,255,0.06)` |

### Шрифт
`Inter` — Google Fonts

### Компоненты (переиспользуемые стили):
```js
// Карточка
const card = {
  background: '#12121a',
  border: '1px solid rgba(255,255,255,0.06)',
  borderRadius: 12, padding: 20, marginBottom: 16
};

// Кнопка
const btn = (active) => ({
  padding: '6px 14px', borderRadius: 6,
  border: '1px solid ' + (active ? '#3b82f6' : 'rgba(255,255,255,0.1)'),
  background: active ? 'rgba(59,130,246,0.2)' : 'transparent',
  color: active ? '#3b82f6' : '#a0a0b0',
  cursor: 'pointer', fontSize: 13, fontWeight: 500
});

// Инпут
const inputStyle = {
  width: '100%', background: '#1a1a2e',
  border: '1px solid rgba(255,255,255,0.1)',
  borderRadius: 8, padding: '10px 14px',
  color: '#e0e0e0', fontSize: 14, outline: 'none'
};

// Бейдж
const badge = (color) => ({
  display: 'inline-block', padding: '3px 10px',
  borderRadius: 6, fontSize: 12, fontWeight: 600,
  background: color === 'green' ? 'rgba(34,197,94,0.15)' :
              color === 'red' ? 'rgba(239,68,68,0.15)' : 'rgba(234,179,8,0.15)',
  color: color === 'green' ? '#22c55e' :
         color === 'red' ? '#ef4444' : '#eab308'
});
```

---

## 13. Все API эндпоинты

### Существующие
| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/klines` | Свечи (прокси Binance) |
| GET | `/api/ticker24h` | Тикеры 24ч топ-10 |
| GET | `/api/fng` | Индекс страха |
| GET | `/api/signals` | Сигналы из БД |
| POST | `/api/signals` | Сохранить сигнал |
| POST | `/api/ai/analyze` | AI анализ монеты |
| POST | `/api/ai/chat` | AI чат |
| GET | `/api/download-project` | Скачать ZIP |

### Нужно добавить
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/auth/register` | Регистрация |
| POST | `/api/auth/login` | Вход |
| GET | `/api/trades` | Список сделок |
| POST | `/api/trades` | Открыть сделку |
| POST | `/api/trades/:id/close` | Закрыть сделку |
| GET | `/api/trades/stats` | Статистика |
| GET | `/api/whale/orderbook` | Книга ордеров |
| GET | `/api/whale/trades` | Крупные сделки |
| GET | `/api/alerts` | Алерты |
| POST | `/api/alerts` | Создать алерт |
| DELETE | `/api/alerts/:id` | Удалить алерт |
| GET | `/api/alerts/triggered` | Сработавшие |
| GET | `/api/news` | Новости |
| POST | `/api/news/summary` | AI саммари |
| GET | `/api/watchlist` | Watchlist |
| POST | `/api/watchlist` | Добавить |
| DELETE | `/api/watchlist/:id` | Удалить |
| GET | `/api/global` | Глобальные данные (CoinGecko) |

---

## 14. Деплой и запуск

```bash
# Клонировать
git clone https://github.com/Kotvuk/KotvukAI_v2.git
cd KotvukAI_v2

# Установить зависимости
npm install

# Собрать фронтенд
npx vite build

# Запустить
node server.js

# Открыть http://localhost:3000
```

### Зависимости (package.json)
```json
{
  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.0",
    "better-sqlite3": "^9.0.0",
    "archiver": "^6.0.0",
    "lightweight-charts": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
```

---

> ✅ **Этот документ содержит ВСЮ информацию для полной доработки KotvukAI. Каждая панель описана с функционалом, API, таблицами БД и дизайном. Следуй структуре и приоритетам.**
